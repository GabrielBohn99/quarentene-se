    <div class="profile-main-box mt-5 d-flex align-items-center justify-content-center">
        <div class="profile-box d-flex flex-column align-items-center justify-content-center">
            <h1>Perfil</h1><br>
            <img class="profile-pic" src="{{user.imgPath}}" alt="Foto de Perfil"><br>
            <h1>Olá {{user.username}}</h1><br>
            <h4>Email: {{user.email}}</h4><br>
            <p>Status da conta: {{user.status}}</p>
            {{#if admin}}
            <a href="/admin" class="btn btn-secondary">Ver postagens pendentes</a>
            {{/if}}
        </div>
    </div>
